name: todolist
description: "todolist baby"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  json_annotation: ^4.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  derry: 1.5.0
  build_runner: ^2.5.4
  json_serializer: 0.2.0

flutter:
  uses-material-design: true
